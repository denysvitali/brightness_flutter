# Brightness Flutter

A minimal Flutter Android app for proving that local and CI compilation work.

## Development

```sh
devenv shell -- flutter pub get
devenv shell -- flutter test
devenv shell -- flutter build apk --debug --no-pub
```
